usage: mpk [-h] [--version] [-i] [-n] [-C] [-e] [-T] [-O] [-t [KIND]]
           [-c [KIND]] [-P [KIND]] [-u [KIND]] [-o VOL] [-p PROFILE] [-S] [-U]
           [-D] [-r] [-w] [-f EXP] [-l CODE] [-L LOC] [-R REPO] [-5]
           [-q QUANTIZER] [-W WIDTH] [--crop T:B:L:R] [--NTSC] [--PAL]
           [--shift TIME] [--input-rate RATE] [--output-rate RATE] [--clean]
           [--show MAP] [--poster MAP] [--initialize] [--conf PATH] [-d]
           [-v LEVEL]
           [PATH [PATH ...]]

positional arguments:
  PATH                  file or directory paths to scan

optional arguments:
  -h, --help            show this help message and exit
  --version             show program's version number and exit

media file processing operations:
  -i, --info            show info
  -n, --rename          rename files to standard names
  -C, --copy            copy into repository
  -e, --extract         extract streams for processing
  -T, --tag             update file tags
  -O, --optimize        optimize file layout
  -t [KIND], --transcode [KIND]
                        one of: m4v, mkv, srt, txt, jpg, png, ac3. defaults to
                        m4v
  -c [KIND], --transform [KIND]
                        one of: m4v. defaults to m4v
  -P [KIND], --pack [KIND]
                        one of: mkv, m4v. defaults to mkv
  -u [KIND], --update [KIND]
                        one of: srt, png, jpg, txt. defaults to srt

media processing modifiers:
  -o VOL, --volume VOL  explicit volume to use
  -p PROFILE, --profile PROFILE
                        explicit profile to use
  -S, --sync            sync encountered records with online service
  -U, --reindex         rebuild physical file index
  -D, --download        download if local is unavailable
  -r, --recursive       recursively process sub directories
  -w, --overwrite       overwrite existing files
  -f EXP, --filter EXP  file name regex filter
  -l CODE, --lang CODE  languge code to use when und [default: eng]
  -L LOC, --local LOC   name of local repository
  -R REPO, --repo REPO  name of repository to use
  -5, --md5             verify md5 checksum after copy

video processing:
  -q QUANTIZER, --quality QUANTIZER
  -W WIDTH, --width WIDTH
                        override profile output pixel width
  --crop T:B:L:R        set HandBrake cropping values [default: autocrop]

subtitle processing:
  --NTSC                convert from PAL to NTSC framerate
  --PAL                 convert from NTSC to PAL framerate
  --shift TIME          shift offset in milliseconds
  --input-rate RATE     subtitles decoding frame rate
  --output-rate RATE    subtitles encoding frame rate

environment and repository:
  --clean               Remove orphan references
  --show MAP            map show to tvdb id [tvdb id]:[show name]
  --poster MAP          choose tmdb movie poster [imdb]:[tmdb]
  --initialize          run only once to initialize the system
  --conf PATH           path for external config file
  -d, --debug           only print commands without executing
  -v LEVEL, --verbosity LEVEL
                        logging verbosity level [default: info]
